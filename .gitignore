# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside

# macOS
.DS_Store

# Swift Package Manager. Package.resolved is committed on purpose — it pins
# exact dependency versions so a fresh clone builds what you built.
.build/
.swiftpm/

# Secrets / local config (keep real keys out of git)
Secrets.xcconfig
*.local.xcconfig

# Firebase — each clone supplies its own project config
GoogleService-Info.plist
